"""RESGA / SAEGA: interpretable persona control via fluent gradient ascent.

This package holds the shared library used by the unified driver (`main.py`),
the sweep launcher (`sweep.py`) and the baselines. The fluent gradient-ascent
optimizer itself lives in the vendored `dreamy` package.
"""
